---
title: "Fireweave"
description: "Fireweave SDK docs: evaluate control points and register targets in Node, Python, Go, Java, Rust, and the browser."
---

Fireweave SDKs evaluate **control points** and optionally register **targets**. Your application authenticates with a Fireweave project key and talks to **fw-server**. The wire protocol and canonical envelopes still use the parameter name `flagKey`; the product name is control point.

v1 exposes exactly those two capabilities. The SDKs do not ship OpenFeature providers, and they do not expose `releases`, `exposures`, `signals`, `capabilities`, or `guardrails`.

<Card title="Quickstart" icon="rocket" href="/quickstart">
  Install, initialize with a required mode, evaluate a control point, register a target, and shut down.
</Card>

## SDKs

Seven language bindings share one surface: a mode-selected adapter, a runtime, and a client with `controlPoints` (cased per language) plus `registerTarget`.

<Columns cols={2}>
  <Card title="Node.js, Bun, and Deno" icon="node-js" href="/sdks/node">
    `@fireweaveai/server-sdk` **2.3.0** — `initFireweave`, `controlPoints`, `registerTarget`. Node `>=20.20`.
  </Card>
  <Card title="Python" icon="python" href="/sdks/python">
    `fireweave` **2.3.0** — `init_fireweave`, `control_points`, `register_target`. Python `>=3.10`.
  </Card>
  <Card title="Go" icon="golang" href="/sdks/go">
    `…/sdks/go/v2` **v2.3.0** — `Init`, `ControlPoints()`, `RegisterTarget`. The `/v2` suffix is required.
  </Card>
  <Card title="Java" icon="java" href="/sdks/java">
    `ai.fireweave:fireweave-sdk` **2.3.0** — `Fireweave.init`, `controlPoints()`, `registerTarget()`. Java 11+.
  </Card>
  <Card title="Browser" icon="globe" href="/sdks/web">
    `@fireweaveai/web-sdk` **2.3.0** — synchronous reads, `registerTarget` / `identify`, `fw_public_` keys.
  </Card>
  <Card title="Rust" icon="gear" href="/sdks/rust">
    crates.io `fireweave` **2.3.0** — `init_fireweave`, `control_points`, `register_target`. Rust 1.75+.
  </Card>
  <Card title="Compatibility" icon="table" href="/sdks/compatibility">
    Seven-language surface, 65×7 conformance aggregate, and the capability matrix.
  </Card>
  <Card title="Package index" icon="cubes" href="/reference/packages">
    Registry names, versions, and what is actually installable.
  </Card>
</Columns>

## What you can do

- **Evaluate** boolean, string, number, and object decisions through nine methods. A read returns your default on failure and **does not throw**.
- **Register targets** once at login (`registerTarget` / language equivalent) so targeting rules can match durable properties.
- **Choose a mode** at init: `remote` (fw-server) or `local` (seeded in-process map). `mode` is required and never inferred.

<Note>
  The product noun is **control point**. `flagKey` remains the key name on the wire, in canonical envelopes, in conformance fixtures, and in `capabilities.features.flags`.
</Note>

## Architecture and concepts

<Columns cols={2}>
  <Card title="How it works" icon="diagram-project" href="/introduction/how-it-works">
    Evaluate and register — the runtime path from your app to fw-server.
  </Card>
  <Card title="Architecture" icon="sitemap" href="/introduction/architecture">
    Client → Runtime → Adapter, lifecycle states, and the fw-server paths.
  </Card>
  <Card title="Core concepts" icon="book" href="/concepts">
    Control points, targeting, adapters, and decisions.
  </Card>
  <Card title="Migrate to v1" icon="code-compare" href="/migration/v1">
    Package rename, required `mode`, and the retired OpenFeature / extension surface.
  </Card>
</Columns>

## Production, troubleshooting, reference

<Columns cols={2}>
  <Card title="Configuration and auth" icon="key" href="/production/configuration">
    Explicit `apiUrl` / `apiKey` (or language equivalent). The SDK reads no environment variables.
  </Card>
  <Card title="Lifecycle" icon="circle-play" href="/production/lifecycle">
    Initialize, READY / STALE, shutdown. Reads after close return defaults.
  </Card>
  <Card title="Errors" icon="circle-exclamation" href="/production/errors">
    Fifteen error kinds and how reads degrade to your default.
  </Card>
  <Card title="Troubleshooting" icon="wrench" href="/troubleshooting">
    Defaults that look like “off,” missing targeting keys, and boot configuration errors.
  </Card>
  <Card title="HTTP API" icon="brackets-curly" href="/reference/http-api">
    `POST /v1/flags/evaluate`, `POST /v1/capture`, `POST /v1/targets/register`.
  </Card>
  <Card title="CLI" icon="terminal" href="/cli/overview">
    Command-line tools for Fireweave projects.
  </Card>
</Columns>

Start with the [Quickstart](/quickstart) if you want a first evaluate in one sitting.
